import React from 'react'
import Header from '../header/Header'

const About = ({counter}) => {
  return (
    <div>
        <Header counter={counter}/>
        About
    </div>
  )
}

export default About